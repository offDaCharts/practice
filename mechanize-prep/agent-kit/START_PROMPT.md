# Start Prompt

> Paste this into Claude Code (or Cursor) as the first message. Replace `<...>` placeholders.

---

We're doing a timed 3-hour take-home interview. Before doing anything, **read `CLAUDE.md` in this directory** — it defines the workflow you must follow.

The take-home prompt is in `<README.md / TASK.md / wherever they put it>`. Read it carefully (twice).

Then:

1. Fill in `PLAN.md` based on the prompt. Stop and show me the plan.
2. **Do not touch any code yet.** Wait for my approval.

Constraints I want you to internalize:
- I must be able to explain every line in a code review tomorrow.
- We have ~3 hours; ship a working ugly thing first, polish later.
- Update `DECISIONS.md` as we go (this is my review-prep cheat sheet).
- Commit every 15–20 minutes.

Banned libraries from the prompt: `<fill in: e.g. tldraw, yjs>`

Tech stack: React frontend + FastAPI backend + Docker.

Go.
