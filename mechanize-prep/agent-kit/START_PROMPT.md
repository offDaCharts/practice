# Start Prompt

> Paste this as the first message in a new Claude Code session. Replace `<...>` placeholders.

---

Before doing anything, **read `CLAUDE.md` in this directory** — it defines how I work here.

The task is described in `<TASK.md / README.md / wherever>`. Read it carefully, then:

1. Fill in `PLAN.md` with a short plan (restated requirements, architecture sketch, data model, deferred items).
2. Stop and show me the plan. Don't write any code yet.

Project-specific context:
- **Stack:** `<e.g., React + FastAPI + Docker>`
- **Banned libraries:** `<e.g., tldraw, yjs>`
- **Time budget (if any):** `<e.g., ~3 hours, or "no rush">`

Go.
